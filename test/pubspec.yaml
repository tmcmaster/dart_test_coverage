name: dart_test_coverage
description: A library for creating a test coverage report.
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=2.12.0 <3.0.0"

dev_dependencies:
  test:
  coverage:
  test_cov: